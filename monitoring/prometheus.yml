global:
  scrape_interval: 5s # 5초마다 메트릭 수집
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'potential-application'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080'] # 로컬에서 실행 중인 앱에 접속 (Docker 환경에 따라 변경 필요 가능)
