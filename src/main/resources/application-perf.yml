spring:
  flyway:
    locations: classpath:db/migration,classpath:db/migration-perf

payment:
  gateway:
    stub:
      enabled: true
      get-payment-delay: 120ms
      cancel-payment-delay: 250ms
